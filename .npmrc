# Fuerza el gestor de paquetes declarado en package.json (pnpm). npm falla al instalar.
engine-strict=true
# Evita que pnpm eleve dependencias al node_modules raíz de forma inconsistente entre máquinas.
auto-install-peers=true
