# src

Se inicializa con Vite + React en la fase 0 (pendiente). Estructura objetivo:

```text
lib/supabaseClient.js     único cliente Supabase (coordinador)
components/               componentes compartidos (coordinador)
services/areaN/           acceso a datos por área
modules/areaN-<slug>/     pantallas por área
App.jsx                   menú principal, una ruta por área
```
